#include <stdio.h>
int main()
{
    int n,a=0,b;
    scanf("%d",&n);
    while (n != 0)
   {
      a = a * 10;
      a = a + n%10;
      n = n/10;
   }
 
   printf("Reverse of entered number is = %d\n",a);
 
   return 0;
}
