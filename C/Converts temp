#include <stdio.h>

int main()
{
    float celsius, fahrenheit;
    char choice;
    
    printf("Choose the conversion for temperature");
    printf("\n 1.Enter C for Celsius to Fahrenheit ");
    printf("\n 2.Enter F for Fahrenheit to Celsius ");
    scanf("%c,&choice");   
    
    if (choice == 'C' || choice =='c' )
    {
       printf("Enter temperature in Celsius: ");
       scanf("%f", &celsius);
       
       fahrenheit = (celsius * 9 / 5) + 32;
       
       printf("The conversion is /n");
       printf("%.5f Celsius = %.5f Fahrenheit", celsius, fahrenheit);
    }
    else if (choice == 'F' || choice =='f' )
    {
       printf("Enter temperature in Fahrenheit: ");
       scanf("%f", &Fahrenheit);
       
       celsius = (fahrenheit − 32) x 5/9 ;
       
       printf("The conversion is /n");
       printf("%.5f Fahrenheit = %.5f Celsius ", fahrenheit, celsius);
    }
    else 
        printf("Wrong entry");
    


    return 0;
}
