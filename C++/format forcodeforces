   #include<bits/stdc++.h>
  #define ll long long
#define pb  push_back
#define fr(i,a,n) for(ll i=a;i<n;i++)
using namespace std;


void solve(){
  int T=1;
 // cin>>T;
  while(T--){
      ll n,a,b,c,d,k,ans,p,m=1,t,z;
      cin>>n>>m;
 //   ans=n/c +((n/c)/a)*b;
        if(m<n-1)
      cout<<n-m-1<<" ";
      else  cout<<"0 ";
      z=sqrt(1+8*m);
      d = (z+2)/(2);
      cout<<n-d<<"\n";
   
    

  }
  
}
    

    int main(){
      #ifndef ONLINE_JUDGE
    // for getting input from input.txt
    freopen("input.txt", "r", stdin);
    // for writing output to output.txt
    freopen("out1.txt", "w", stdout);
#endif
        ios_base::sync_with_stdio(false);   
        cin.tie(NULL);
       solve();
    return 0;
    } 
