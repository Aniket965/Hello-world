# solution for projecteuler.net problem #63

found = True
i = 1
pow = 1
count = 0

while found == True:
	i = 1
	print pow
	found = False
	while len(str(i**pow)) <= pow:
		if len(str(i**pow)) == pow:
			print str(i) +"**"+str(pow)
			found = True
			count += 1
		i += 1
	pow += 1

print
print count
