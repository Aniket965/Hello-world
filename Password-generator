import java.util.Random;

import javax.swing.JOptionPane;

classe publique Main {
	public static void main (String Args []) {
		int c = Integer.parseInt (JOptionPane.showInputDialog ("Un mot de passe de combien de caractères?"));
		String password = "";
		Aléatoire r = nouveau Aléatoire ();
		pour (int i = 0; i <c; i ++) {
			mot de passe + = (char) (97 + r.nextInt (25));
		}

		JOptionPane.showMessageDialog (null, mot de passe);
	}
}
